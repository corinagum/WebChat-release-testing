{
  "name": "esbuild-web-chat",
  "version": "0.0.0-0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle --entry-names=[ext]/[name] --format=cjs --jsx=automatic --minify --outdir=./public/static/ --sourcemap app=./src/app/index.tsx",
    "start": "npm run build -- --watch"
  },
  "author": "William Wong (https://github.com/compulim)",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.5",
    "@types/react": "^16.14.60",
    "@types/react-dom": "^16.9.24",
    "esbuild": "^0.20.2"
  },
  "dependencies": {
    "punycode": "^2.3.1",
    "react": "~16.8.6",
    "react-dom": "~16.8.6"
  }
}
